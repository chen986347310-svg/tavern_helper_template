const WS = require(" ws\); const fs = require(\fs\); const ws = new WS(\ws://localhost:9222/devtools/page/A3A31BC3DAE2849299C3D9475930DD77\); ws.on(\open\, () => { ws.send(JSON.stringify({id:1, method:\Page.captureScreenshot\, params:{format:\png\}})); }); ws.on(\message\, (data) => { const obj = JSON.parse(data.toString()); if (obj.id === 1 && obj.result) { fs.writeFileSync(\screenshot_current.png\, Buffer.from(obj.result.data, \base64\)); console.log(\Screenshot saved\); ws.close(); process.exit(0); } }); ws.on(\error\, (err) => { console.error(err.message); process.exit(1); });
