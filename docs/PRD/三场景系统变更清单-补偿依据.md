# 历史留痕说明

本文档记录的是已废弃的三场景方案与补偿依据，不作为当前实现约束。当前 v4 已升级为开放动态场景系统：系统.当前场景 与 NPC.*.当前场景 均为开放字符串，莲灯前苑 / 醉玉小筑 / 绮梦幽阁 仅保留为快捷入口、默认文案与旧数据回退。

# 三场景系统变更清单 — 补偿依据文档

> **变更原因**：甲方在项目中后期提出"只有三个场景并不符合需求"，导致此前已完成的三场景系统全部需要重新评估。本文档详细列举三场景系统在代码、测试、世界书、文档各层的全部已完成工作，作为  补偿的工程量依据。

> **文档日期**：2026-05-22
> **变更范围**：29 个文件、341 处引用、198 行涉及代码/测试/配置

---

## 一、变更总览

| 层级 | 涉及文件数 | 涉及引用数 | 说明 |
|------|-----------|-----------|------|
| 数据 Schema | 3 | 17 | schema.ts / schema.json / initvar.yaml |
| 世界书规则 | 3 | 10 | 变量列表 / 输出格式 / 更新规则 |
| 前端组件 | 4 | 12 | App / SystemBar / HomePage / usePendingAction |
| 游戏逻辑 | 2 | 35 | guards.ts（场景守卫） / whispers.ts（心音文案） |
| 测试 | 8 | 129 | 场景守卫 / 心音 / 首页 / App / 背包 / 商城 / 调试 / 待处理交互 |
| 文档 | 7 | 45 | PRD / 架构报告 / 开发规范 / 美学方案 / 架构指南 / handoff |
| **合计** | **29** | **341** | |

---

## 二、数据 Schema 层（3 文件 / 17 处引用）

### 2.1 schema.ts（3 处）

- 系统级场景枚举：当前场景: z.enum(['莲灯前苑', '醉玉小筑', '绮梦幽阁'])
- 待处理交互内嵌场景枚举：场景: z.enum(['莲灯前苑', '醉玉小筑', '绮梦幽阁'])
- NPC 级场景枚举：当前场景: z.enum(['莲灯前苑', '醉玉小筑', '绮梦幽阁'])

### 2.2 schema.json（14 处）

- 自动生成的 JSON Schema，3 处 enum 定义 + 3 处 default 值 + 8 处枚举项展开

### 2.3 initvar.yaml（1 处）

- 默认场景初始化：当前场景: 莲灯前苑

---

## 三、世界书规则层（3 文件 / 10 处引用）

### 3.1 变量列表.yaml（3 处）

- 系统级场景定义：当前场景: "莲灯前苑" | "醉玉小筑" | "绮梦幽阁"
- 待处理交互场景定义：场景: "莲灯前苑" | "醉玉小筑" | "绮梦幽阁"
- NPC 级场景定义：当前场景: "莲灯前苑" | "醉玉小筑" | "绮梦幽阁"

### 3.2 变量输出格式.yaml（4 处）

- 灵识窃取样例中的场景字段：当前场景: 醉玉小筑、场景: 醉玉小筑（出现 2 次，用于输入/输出样例）

### 3.3 变量更新规则.yaml（2 处，核心规则）

- 场景感知交互规则："每条交互都带有 类型、目标、道具、数量、时辰、场景，叙事必须参考这些字段"
- 灵识窃取场景规则："应根据目标NPC的攻略值、当前场景、soul_whisper阶段写出成功窥见、心防反震或心音残片"
- 使用物品场景规则："使其法效在当前场景中显现"

---

## 四、前端组件层（4 文件 / 12 处引用）

### 4.1 App.vue（2 处核心逻辑）

- 场景状态管理：const currentScene = ref<SceneName>((store.data.系统.当前场景 as SceneName) ?? '莲灯前苑')
- 场景切换函数：unction switchScene(scene: SceneName) { currentScene.value = scene; }
- SystemBar 组件传入 currentScene prop + @sceneChange 事件
- HomePage 组件传入 currentScene prop

### 4.2 SystemBar.vue（4 处，含 UI 渲染）

- 场景切换器 UI：3 个按钮，每个含 glyph + name
- 场景列表数据：{ name: '莲灯前苑', glyph: '苑' } / { name: '醉玉小筑', glyph: '玉' } / { name: '绮梦幽阁', glyph: '阁' }
- SceneName 类型定义 + Props/Emit 类型约束
- CSS 样式：.scene-switcher / .scene-btn / .scene-name / .scene-glyph

### 4.3 HomePage.vue（4 处，含场景过滤逻辑）

- 场景类型定义：	ype SceneName = '莲灯前苑' | '醉玉小筑' | '绮梦幽阁'
- 场景空态文案：3 条宗门语境的氛围留白文本
- NPC 过滤逻辑：const visibleNpcs = computed(() => get场景NPC列表(data.NPC, props.currentScene))
- 当前场景配置：const currentSceneConfig = computed(() => 场景列表.find(...))
- CSS 样式：.scene-empty

### 4.4 usePendingAction.ts（2 处）

- 场景类型定义 + 待处理交互结构中的 场景: SceneName 字段
- 自动带入当前场景：场景: payload.场景 ?? store.data.系统.当前场景 ?? '莲灯前苑'

---

## 五、游戏逻辑层（2 文件 / 35 处引用）

### 5.1 guards.ts（16 处，核心场景游戏机制）

**场景感知守卫（Scene-Aware Guards）**：

- 场景类型定义：	ype 场景名 = '莲灯前苑' | '醉玉小筑' | '绮梦幽阁'
- 场景窃听成功率表：莲灯前苑 0.6 / 醉玉小筑 0.75 / 绮梦幽阁 0.9
- 场景反噬倍率表：莲灯前苑 0.5 / 醉玉小筑 1 / 绮梦幽阁 1.5
- get场景NPC列表() — 按场景过滤 NPC，保持攻略链顺序
- get窃听成功率() — 按场景返回成功率
- get反噬倍率() — 按场景返回反噬系数
- perform灵识窃取() — 综合场景成功率 + 粘滞增益 + 阶段判定 + 反噬计算

### 5.2 whispers.ts（18 处，场景感知心音文案库）

- 场景类型定义：	ype SceneName = '莲灯前苑' | '醉玉小筑' | '绮梦幽阁'
- 完整的 5 NPC × 3 场景 × 3 阶段 = **45 条独立心音文案**
- 每条文案均根据场景氛围定制（如"莲灯照影" vs "酒气太甜" vs "门合上了"）
- getRandomWhisper() 函数：按 NPC + 场景 + 阶段 + 索引返回文案

---

## 六、测试层（8 文件 / 129 处引用）

### 6.1 guards.scene.test.ts（19 处，专属场景守卫测试文件）

- NPC 场景分配 mock（5 NPC 各分配到 3 场景之一）
- 场景 NPC 列表过滤测试（验证每个场景只返回对应 NPC）
- 场景窃听成功率测试（3 场景 × 不同成功率）
- 场景反噬倍率测试（3 场景 × 不同倍率）
- perform灵识窃取综合场景测试（成功/失败/粘滞封顶）

### 6.2 whispers.test.ts（3 处）

- 覆盖 5 NPC × 3 场景 × 3 阶段的完整性断言
- 按索引稳定返回非空文本断言

### 6.3 HomePage.test.ts（30 处，场景过滤核心测试）

- 场景 mock 数据：5 NPC 各分配到不同场景
- 场景过滤测试：只渲染当前场景 NPC
- 场景切换测试：切换场景后 NPC 列表变化
- 空场景测试：无 NPC 时显示氛围留白文案
- 灵识窃取场景测试：待处理交互携带场景信息

### 6.4 App.test.ts（11 处）

- 场景 mock 数据：5 NPC 各分配到不同场景
- 顶栏场景切换驱动主页 NPC 列表测试
- .scene-btn 按钮存在性断言

### 6.5 usePendingAction.test.ts（7 处）

- 待处理交互自动带入当前场景测试
- 场景类型断言：场景: '莲灯前苑' | '醉玉小筑' | '绮梦幽阁'

### 6.6 ShopPage.test.ts（2 处）

- 购买特殊场景后加入场景解锁列表测试
- 交互记录携带场景信息断言

### 6.7 BackpackPage.test.ts（3 处）

- 场景 mock 数据 + 交互记录携带场景信息

### 6.8 DebugPanel.test.ts（1 处）

- 调试面板 mock 数据包含场景字段

---

## 七、文档层（7 文件 / 45 处引用）

| 文件 | 引用数 | 主要内容 |
|------|--------|----------|
| docs/PRD/PRD-主页状态栏深度重构v4.md | 34 | 场景约束规则、场景枚举验收、旧场景名迁移记录 |
| docs/前端架构指南.md | 4 | 场景枚举原则、CDP 场景验收流程 |
| docs/教程/开发规范与智能体协作指南.md | 3 | 场景枚举协作约束、术语红线 |
| docs/PRD/vue-状态栏美学设计方案.md | 3 | 场景锚点设计、旧场景名废弃声明 |
| docs/架构报告-界面架构重组.md | 1 | 场景枚举权威结论 |
| docs/handoff/handoff-v4-docs-and-closure-2026-05-22.md | 1 | 场景约束交接说明 |
| docs/PRD/PRD-雌堕合欢宗.md | 1 | 场景系统设计参考 |

---

## 八、已完成的场景迁移工作

在项目迭代过程中，场景命名经历了从旧名到新名的完整迁移：

| 旧场景名 | 新场景名 | 意境 |
|----------|----------|------|
| 喧闹大堂 | 莲灯前苑 | 公开社交、灯影交错 |
| 微醺吧台 | 醉玉小筑 | 半私密、酒香微醺 |
| 幽寂厢房 | 绮梦幽阁 | 极度私密、深度攻略 |

迁移涉及：schema 定义、世界书规则、前端组件、游戏逻辑、测试断言、全部 7 份文档，共计全量替换。

---

## 九、工程量总结

| 维度 | 数量 |
|------|------|
| 涉及文件总数 | 29 |
| 代码/配置引用总数 | 341 |
| 核心函数/组件 | 7 个（App.vue 状态管理、SystemBar.vue 切换器、HomePage.vue 过滤、guards.ts 4 个守卫函数、whispers.ts 心音库、usePendingAction.ts 入队） |
| 测试文件 | 8 个 |
| 心音文案（5×3×3） | 45 条 |
| 游戏机制参数表 | 2 个（窃听成功率 + 反噬倍率） |
| 文档引用 | 7 个文件 45 处 |
| 场景迁移工作量 | 全量替换（旧名 → 新名） |

> **结论**：三场景系统已渗透到项目的 Schema 定义、AI 世界书规则、前端 UI 组件、游戏逻辑守卫、心音文案库、测试套件和全部技术文档中。该系统的变更不是简单的"改几个字符串"，而是涉及数据结构、游戏机制、前端交互、AI 提示词、测试覆盖和文档合规的全栈级重构。