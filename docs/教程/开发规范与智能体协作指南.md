# 开发规范与智能体协作指南

> 面向所有开发者（人类和 AI 智能体）的项目开发规范手册。涵盖开发流程、代码质量审查、智能体协作和 Skill 使用指南。

---

## 当前权威协作口径（2026-05-22）

本节优先级高于后文早期通用流程，所有接手者必须先读完再开工：

- **前端定位**：前端是 MVU 变量的显示端、交互端与少量即时反馈执行端；它可以写入 `系统.待处理交互`，但不得直接代替 AI 生成下一楼层剧情。
- **闭环边界**：UI 点击只负责入队；用户发送下一条消息后，AI 读取队列、叙事承接、输出标准 `<UpdateVariable><JSONPatch>`，再由 MVU 回流到前端。
- **同步断点**：当前 `package.json` 的 `sync` 指向 `node tavern_sync.mjs`，但工作区缺少 `tavern_sync.mjs`；在恢复或替代同步链路前，不得宣称世界书/角色卡已同步到酒馆。
- **验收红线**：`pnpm build`、Vitest 通过只代表前端产物通过；v4 闭环必须额外确认最新楼层输出格式、MVU 最新变量回流和 `系统.待处理交互` 清空。
- **术语红线**：统一使用“牝奴”“太极/命轮”“莲灯前苑 / 醉玉小筑 / 绮梦幽阁”，不得回退为“牡奴”“八卦”或旧场景名。

---
## 一、开发环境与工具链

### 1.1 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| UI 框架 | Vue 3 + Pinia | 组件化界面 + 状态管理 |
| 样式 | SCSS（scoped） | 预处理器，组件私有样式 |
| 校验 | Zod 4 | MVU 变量结构定义与运行时校验 |
| 构建 | Webpack 5 | 打包为自包含单文件 HTML |
| 测试 | Vitest + @vue/test-utils + happy-dom | 单元测试 + 组件测试 |
| 代码质量 | ESLint + Prettier | 静态分析 + 格式化 |
| 包管理 | pnpm | 依赖管理 |

> **注意**：项目安装了 Tailwind CSS 4，但 `tailwind.css` 注释明确标注"仅激活语法高亮，禁止实际使用"。组件样式一律使用 `<style lang="scss" scoped>`。

### 1.2 常用命令

```powershell
# 开发构建（含 sourcemap）
pnpm build:dev

# 生产构建（混淆压缩）
pnpm build

# 监听模式（配合热重载使用）
pnpm watch

# 运行测试
npx vitest run

# 单文件测试
npx vitest run src/雌堕合欢宗/界面/components/NpcCard.test.ts

# ESLint 检查 / 自动修复
pnpm lint
pnpm lint:fix

# Prettier 格式化
pnpm format

# 生成 JSON Schema（用于 initvar.yaml 校验）
pnpm dump

# 同步角色卡/世界书到酒馆（当前不可直接执行；见 12.5 同步断点）
# pnpm sync

# 一键启动开发环境（Chrome 调试 + webpack watch）
.\start-dev.ps1
.\start-dev.ps1 -ChromeOnly   # 仅启动 Chrome
.\start-dev.ps1 -NoKill         # 不关闭现有 Chrome
```

### 1.3 共享工具模块（util/）

| 文件 | 职责 |
|------|------|
| `util/mvu.ts` | `defineMvuDataStore`：基于 Pinia 的 MVU 变量双向同步 |
| `util/script.ts` | 脚本工具：`createScriptIdDiv`、`teleportStyle`、`reloadOnChatChange` |
| `util/streaming.ts` | 流式楼层界面：`mountStreamingMessage` |
| `util/common.ts` | 通用工具函数 |

### 1.4 构建产物验证

构建后必须验证 dist 产物的完整性：

```powershell
# 检查是否有构建错误
Select-String -Path dist\雌堕合欢宗\index.html -Pattern 'Module parse failed'

# 检查文件大小（预期约 6-10 MB，含 10 张压缩后 base64 内联图片 512x512）
 = 'dist\雌堕合欢宗\index.html'
Write-Output "大小: $([math]::Round((Get-Item $f).Length/1MB, 2)) MB"

# 验证所有 entry 产物为单文件（无外部文件泄露）
Get-ChildItem dist -Directory | ForEach-Object {
    $files = (Get-ChildItem $_.FullName -Recurse -File).Count
    Write-Output "$($_.Name): $files 个文件"
}
```

### 1.5 Schema → initvar.yaml 工作流

```
schema.ts (zod 4 定义)
    ↓ pnpm dump
schema.json (JSON Schema)
    ↓ 引用
世界书/变量/initvar.yaml (# yaml-language-server: $schema=schema.json)
```

修改 `schema.ts` 后必须重新执行 `pnpm dump` 以更新 `schema.json`，否则 `initvar.yaml` 的自动校验会失效。

---
### 1.6 Codex CLI 文件写入规范

在 Codex CLI 环境中写文件时，首选**小块、可复查、可回滚**的增量修改。不要为了省事整文件覆盖，尤其不要用默认编码写中文路径或中文正文。

#### 推荐优先级

| 优先级 | 方式 | 适用场景 | 注意事项 |
|------|------|------|------|
| A | `apply_patch` | 小范围改动、可稳定匹配上下文的代码/文档 | patch 必须只描述差异，写后检查是否残留 `*** End Patch` |
| B | PowerShell 原生 UTF-8 + `.NET` API | 中文文档大段替换、当前环境确认 PowerShell UTF-8 正常时 | 使用 `[System.IO.File]::ReadAllText/WriteAllText` 与 `[Text.UTF8Encoding]::new($false)`，禁止默认编码 |
| C | 临时脚本 | 多处结构化替换、需要正则定位时 | 脚本执行后删除临时文件，保留验证命令输出 |

#### PowerShell UTF-8 安全模板

```powershell
$OutputEncoding = [Console]::OutputEncoding = [Text.UTF8Encoding]::new($false)
[Console]::InputEncoding = [Text.UTF8Encoding]::new($false)
$path = 'docs/教程/example.md'
$utf8 = [Text.UTF8Encoding]::new($false)
$text = [IO.File]::ReadAllText($path, $utf8)
$text = $text.Replace('旧内容', '新内容')
[IO.File]::WriteAllText($path, $text, $utf8)
```

#### 禁止做法

- 禁止用 `Set-Content` / `Add-Content` 默认编码写中文文档。
- 禁止用超长单行命令整文件覆盖。
- 禁止把 `apply_patch` 文本通过 shell 当普通命令执行。
- 禁止在含 `$`、反引号、Vue 模板或 SCSS 变量的内容里使用未转义的 PowerShell 双引号。

#### 写后复查

每次写入后至少执行一条定位复查命令，例如：

```powershell
Select-String -Path 'docs/教程/开发规范与智能体协作指南.md' -Pattern '当前权威协作口径|tavern_sync.mjs|UpdateVariable|待处理交互'
```
## 二、开发流程规范

### 2.1 标准开发流程（6 步）

```
需求理解 → 编码实现 → 本地测试 → 构建验证 → 代码审查 → 提交交付
```

#### 第 1 步：需求理解

- 阅读相关 PRD 文档（`docs/PRD/`）
- 阅读架构文档（`docs/架构报告-*.md`、`docs/前端架构指南.md`）
- 明确修改范围：是新增功能、样式调整还是 bug 修复
- 确认涉及的文件和模块（参考 `docs/前端架构指南.md` 的组件依赖图）

#### 第 2 步：编码实现

- 遵循下面「三、代码质量标准」中的所有规则
- 使用现有 SCSS 变量和 mixin，不硬编码颜色/间距
- 新增组件时同步编写对应的 `.test.ts` 文件
- 修改 schema 时确保幂等性（`Schema.parse(Schema.parse(x)) === Schema.parse(x)`）

#### 第 3 步：本地测试

```powershell
# 运行所有测试
npx vitest run

# 运行特定模块测试
npx vitest run src/雌堕合欢宗/界面/guards.test.ts
```

#### 第 4 步：构建验证

```powershell
# 清理旧产物
Remove-Item dist\雌堕合欢宗 -Recurse -Force

# 生产构建
pnpm build

# 验证产物（见 1.4 节）
```

#### 第 5 步：代码审查

按照「四、代码审查清单」逐项检查。

#### 第 6 步：提交交付

- 确保 dist 产物无构建错误
- 编写或更新 handoff 文档（`docs/handoff/`）
- 记录已知问题和下一步建议

---

### 2.2 热重载开发流程（推荐）

日常开发推荐使用热重载模式，无需每次手动 `pnpm build`：

```
1. 启动监听模式: pnpm watch（或用 .\start-dev.ps1 一键完成步骤 1-2）
2. 启动带调试端口的 Chrome（参见 docs/教程/chrome调试启动指引.md）
3. 通过 MCP chrome-devtools 连接酒馆网页
4. 确认「酒馆助手-实时监听-允许监听」开关已启用
5. 修改 src/ 代码后，酒馆网页自动热重载
```

验证热重载是否生效：打开酒馆网页后，检查 `$("#extensions_settings")` 中的「酒馆助手-实时监听-允许监听」开关是否启用。

---
### 2.3 MVU 角色卡开发流程（额外步骤）

当开发 MVU 角色卡相关功能时，在标准流程基础上增加以下步骤：

```
需求理解 → schema 设计 → 变量同步 → 界面实现 → 脚本实现 → 世界书编写 → 测试 → 构建
```

#### 角色卡文件结构

```
src/雌堕合欢宗/
  index.ts              # 入口文件
  index.html            # HTML 模板（仅 <body> 静态内容）
  App.vue               # 根组件
  schema.ts             # MVU 变量结构定义（zod 4）
  schema.json           # 自动生成的 JSON Schema
  index.yaml            # 角色卡元数据（头像、描述、世界书引用）
  界面/                 # 前端界面项目
    components/         # Vue 组件
    composables/        # 组合式函数
    pages/              # 页面组件
    styles/             # SCSS 样式
    assets/avatars/     # NPC 立绘图片
    data/               # 静态数据
    store.ts            # Pinia store
    guards.ts           # 游戏逻辑函数
  脚本/                 # 后台脚本项目
    MVU/                # MVU 相关脚本
    变量结构/            # 变量结构脚本
    后端校验/            # 后端校验脚本
  世界书/               # 世界书条目
    变量/               # 变量相关条目
    NPC/                # NPC 专属条目（每人一个 yaml）
    玩家/               # 玩家相关条目
    *.yaml              # 世界设定、宗门文化等
  第一条消息/           # 角色卡首条消息
    0.txt
```

#### schema 设计检查

- [ ] `schema.ts` 使用 zod 4（不是 zod 3）
- [ ] 所有字段有 `.prefault()` 默认值
- [ ] 数字字段用 `z.coerce.number()` + `_.clamp()` 限制范围
- [ ] 使用 `z.record()` 而非 `z.array()` 管理动态集合
- [ ] `Schema.parse(Schema.parse(x)) === Schema.parse(x)` 幂等性成立
- [ ] 修改后执行 `pnpm dump` 更新 `schema.json`

#### 三处同步检查

新增 NPC 时必须同步修改 3 处：
1. `_variables.scss`：`$npc专属色` map 加新颜色
2. `schema.ts`：`z.enum` 中加新名字
3. `guards.ts`：攻略链数组加新名字

可选同步：
4. `世界书/NPC/`：新增 NPC 专属世界书条目
5. `界面/assets/avatars/`：新增正道版 + 仙奴版立绘图片
6. `index.yaml`：更新角色卡元数据

---

## 三、代码质量标准

### 3.1 TypeScript 规范

| 规则 | 说明 |
|------|------|
| 禁止 `any` | 除非有充分理由并加注释 |
| 禁止未使用变量 | `noUnusedLocals` + `noUnusedParameters` 已在 tsconfig 启用 |
| 禁止隐式返回 | `noImplicitReturns: true` |
| 优先使用 `const` | ESLint `prefer-const: warn` |
| 优先使用酒馆助手接口 | `@types/function/*.d.ts` 优先于 `exported.sillytavern.d.ts` |
| 使用 `klona()` 脱 proxy | 写入酒馆变量前必须 `klona()` 去除 Vue 响应式代理 |

### 3.2 Vue 组件规范

- 使用 `<style lang="scss" scoped>`，禁止全局样式
- 禁止使用 `vh` 单位（iframe 环境下不可靠），使用 `width` + `aspect-ratio`
- 禁止 `position: absolute` 使主体脱离文档流
- 禁止在 `index.html` 中使用 `<link>` 或 `<script>` 引入外部文件
- 图片使用相对路径（无 `/` 开头），适配 iframe 子目录环境

### 3.3 SCSS 规范

- 颜色值使用 `_variables.scss` 中定义的 token
- 可复用视觉效果使用 `_mixins.scss` 中的 mixin
- 禁止在 mixin 中硬编码可被 CSS 变量覆盖的颜色（如需暗/亮双模式）
- 需要主题感知的元素使用 `var(--theme-*)` 或 `var(--npc-accent)`
- 需要覆写 mixin 硬编码色时，在 `_global.scss` 的 `html[data-theme=light]` 块中使用 `!important`

### 3.4 测试规范

- 测试框架：Vitest + @vue/test-utils + happy-dom
- **环境配置**：`vitest.config.ts` 全局设为 `environment: "node"`，各测试文件通过顶部 `// @vitest-environment happy-dom` 指令覆盖
- 只测外部行为，不测实现细节
- 每个组件/模块的 `.test.ts` 文件与源文件同目录
- `beforeEach` 中重置 composable 共享状态（模块级 `ref`/`reactive`）
- happy-dom 限制：修饰键需用 `Object.defineProperty` 强制设置，中文文件名需 `decodeURIComponent()`

### 3.5 ESLint 配置要点

项目 `eslint.config.mjs` 启用的规则：

- `import-x/no-cycle: error`：禁止循环依赖
- `no-var: error`：禁止 `var`
- `yoda: error`：条件表达式常量在左侧
- `pinia` 插件：Pinia store 规范
- `better-tailwindcss`：类名规范（已安装但主要项目不用 Tailwind）
- `vue` 插件：Vue SFC 规范

---

## 四、代码审查清单

### 4.1 提交前必查（P0）

| 检查项 | 命令/方法 | 通过标准 |
|--------|-----------|----------|
| TypeScript 编译 | `pnpm build:dev` | 无错误 |
| ESLint | `pnpm lint` | 无 error |
| 单元测试 | `npx vitest run` | 全部通过 |
| dist 无构建错误 | `Select-String` 搜索 `Module parse failed` | 无匹配 |
| dist 单文件 | 检查目录文件数 | 每个 entry 仅 1 个 index.html |
| apply_patch 残留 | 搜索 `*** End Patch` | 无匹配 |

### 4.2 功能正确性审查（P1）

- [ ] 新功能按预期工作（手动测试或截图验证）
- [ ] 修改不影响已有功能（回归测试）
- [ ] MVU 变量读写双向同步正常
- [ ] 主题切换（暗/亮）下样式均正常
- [ ] iframe 高度自适应，无横向滚动条

### 4.3 代码质量审查（P1）

- [ ] 无硬编码颜色值（应使用 SCSS 变量或 CSS 自定义属性）
- [ ] 无重复代码（提取 mixin 或 composable）
- [ ] 组件 props 有明确类型定义
- [ ] 新增的 composable 是模块级单例（如需共享状态）
- [ ] 没有 `process.env.NODE_ENV === "development"` 条件守卫（会被 tree-shake）

### 4.4 架构一致性审查（P2）

- [ ] 导航变更同步修改 App.vue + PageNav.vue + useNavigation
- [ ] 新 NPC 同步修改 _variables.scss + schema.ts + guards.ts（3 处）
- [ ] 新页面联动 App.vue v-if + PageNav tabs
- [ ] 数据变更同步 schema.ts + store.ts

---

## 五、智能体协作开发指南

### 5.1 何时使用智能体

| 场景 | 推荐方式 | 原因 |
|------|----------|------|
| 新功能开发（涉及多文件） | 并行子智能体 | 拆分为独立模块并行开发 |
| Bug 排查 | 单智能体 + `diagnose` skill | 系统化排查流程 |
| UI 样式调整 | 单智能体 + `ui-ux-pro-max` skill | 专业样式建议 |
| 代码审查 | 子智能体 + `review` skill | 独立视角审查 |
| PRD → 代码 | 单智能体 + `writing-plans` + `executing-plans` | 规划后再执行 |
| 截图验证 | `browser-use` skill | 自动化 UI 截图对比 |

### 5.2 并行子智能体模式

当任务可拆分为多个独立模块时，使用 `dispatching-parallel-agents` 模式：

```
主智能体
  子智能体 1: 新增 ShopPage 样式
  子智能体 2: 新增 BackpackPage 样式
  子智能体 3: 新增 GalleryPage 样式
```

拆分原则：
- 每个子智能体拥有独立的文件写入范围（不重叠）
- 共享文件（如 `_variables.scss`、`_global.scss`）由主智能体统一处理
- 子智能体之间不需要互相等待

### 5.3 信息传递格式（Handoff）

当一个智能体需要将工作交接给另一个时，使用 `docs/handoff/` 下的 handoff 文档：

```markdown
# Handoff

## Context
- 技术栈、运行环境、当前测试状态

## Done
- 已完成的工作列表

## BUG / 未解决问题
- 问题描述 + 已排查的线索

## Files
| 文件 | 路径 |
|------|------|

## Next
- 下一步建议

## Skills
- 推荐使用的 skill
```

---
> **与 ``handoff`` skill 的关系**：上面的模板是本项目的扩展版 handoff 格式，比 ``handoff`` skill 自带的通用模板多了 ``Files`` 和 ``Skills`` 两个章节。``handoff`` skill（位于 ``C:/Users/98634/.codex/skills/handoff/SKILL.md``）会生成一份精简的交接文档到临时文件，适合快速交接；而项目模板保存在 ``docs/handoff/`` 下，适合需要留存记录的正式交接。两种方式可以结合使用：先用 ``handoff`` skill 快速生成草稿，再手动补充 Files 和 Skills 信息后保存到 ``docs/handoff/``。
## 六、Skill 使用指南

### 6.1 开发阶段 Skill 映射

| 开发阶段 | 推荐 Skill | 用途 |
|----------|-----------|------|
| 需求分析 | `grill-me`, `plan-ceo-review` | 挑战需求、明确范围 |
| 方案设计 | `plan-eng-review`, `plan-design-review`, `plan-devex-review` | 工程/设计/体验评审 |
| 编码实现 | `test-driven-development`, `tdd` | TDD 驱动开发 |
| 样式开发 | `ui-ux-pro-max`, `frontend-design` | UI 设计规范和实现 |
| 样式审查 | `design-review`, `web-design-guidelines` | 视觉质量和可访问性审查 |
| Bug 排查 | `diagnose`, `systematic-debugging`, `investigate` | 系统化调试 |
| 代码审查 | `review`, `requesting-code-review` | diff 审查和质量检查 |
| 测试 | `qa`, `qa-only`, `webapp-testing` | 自动化 QA 测试 |
| 截图验证 | `browser-use:browser` | 浏览器截图和交互验证 |
| 构建修复 | 参考 `docs/教程/前端构建修复指南.md` | webpack 构建问题排查 |
| 文档更新 | `document-release`, `document-generate` | 生成/更新文档 |
| 上下文保存 | `context-save`, `context-restore` | 跨会话恢复工作状态 |
| Handoff | `handoff` | 生成交接文档 |

### 6.2 Skill 调用规范

触发规则：当用户明确提到 skill 名称，或任务明显匹配 skill 描述时，必须使用该 skill。

加载方式：
1. 根据 skill 列表找到对应的文件路径
2. 读取 `SKILL.md` 文件了解工作流程
3. 按照流程执行

常用 Skill 快速参考：

```
代码审查:  $review → 读取 SKILL.md → 分析 diff → 给出 pass/fail
Bug 排查:  $diagnose → 读取 SKILL.md → 复现 → 最小化 → 假设 → 修复 → 回归
UI 截图:   $browser-use:browser → 打开 localhost → 截图 → 对比
上下文保存: $context-save → 保存 git 状态 + 决策 + 剩余工作
上下文恢复: $context-restore → 恢复上次会话状态
```

### 6.3 Skill 组合工作流示例

#### 场景：开发新页面样式

```
1. writing-plans          → 制定实施计划
2. test-driven-development → 编写测试
3. ui-ux-pro-max           → 设计样式方案
4. 编码实现                 → 按计划实现
5. review                  → 代码审查
6. browser-use             → 截图验证
7. document-release        → 更新文档
```

#### 场景：修复 Bug

```
1. investigate            → 系统化排查
2. diagnose               → 定位根因
3. 编码修复               → 实现修复
4. npx vitest run         → 测试验证
5. browser-use            → 视觉验证
6. context-save           → 记录修复过程
```

---

## 七、常见陷阱速查

| 陷阱 | 症状 | 解决 |
|------|------|------|
| `apply_patch` 残留 | SCSS/TS 编译失败 | 检查文件末尾删除 `*** End Patch` |
| 正则转义错误 | 二进制文件解析失败 | 检查 `\$` vs `$` |
| `asset/resource` vs `asset/inline` | iframe 无法加载外部文件 | 使用 `asset/inline` |
| composable 非单例 | 快捷键/面板状态不同步 | 提取 `ref` 到模块顶层 |
| `process.env.NODE_ENV` 守卫 | 功能在生产构建消失 | 移至 `onMounted` |
| happy-dom 修饰键 | 快捷键测试失败 | `Object.defineProperty` 强制设置 |
| CSS 特异性竞争 | 样式覆盖不生效 | 提高选择器特异性或用 `!important` |
| `color-mix()` + `var()` | 部分浏览器异常 | 用 JS 双色映射替代 |
| 全局字符串替换破坏主题 | 暗/亮模式文字不可见 | 替换前用正则确认匹配位置 |
| 中文文件名 URL 编码断言 | 测试中文件名匹配失败 | 先 `decodeURIComponent()` 再断言 |
| PowerShell `$` 插值 | apply_patch 中 `$variable` 被替换为空 | 使用 Python 脚本编辑含 `$` 的文件（SCSS/Shell） |

---

## 八、文档维护规范

### 文档目录结构

```
docs/
  PRD/                      # 产品需求文档
    PRD-雌堕合欢宗.md           # 主 PRD
    PRD-界面架构重组.md          # 架构重组 PRD
    vue-状态栏美学设计方案.md     # 状态栏美学 PRD
    角色立绘提示词.md            # 立绘美术参考
  教程/                     # 开发教程和指南
    开发规范与智能体协作指南.md  <- 本文档
    前端构建修复指南.md          # webpack 构建问题排查
    chrome调试启动指引.md       # Chrome 调试教程
    状态栏正则解答.md           # 状态栏正则 Q&A
  handoff/                  # 会话交接文档
    detailed-execution-plan.md  # 执行计划（阶段标记完成）
  架构报告-界面架构重组.md      # 架构详细报告
  前端架构指南.md              # 快速上手指南
```

### 何时更新文档

| 事件 | 需更新的文档 |
|------|-------------|
| 新增页面/组件 | `前端架构指南.md` 组件依赖图 |
| 修改架构 | `架构报告-*.md` |
| 新增陷阱 | `前端构建修复指南.md` |
| 新增 MVP 字段 | `架构报告` 数据流章节 |
| 完成开发交接 | `handoff/*.md` |
| PRD 变更 | `PRD/*.md` |

---

## 九、快速诊断命令集

```powershell
# 一键诊断：构建错误 + 文件大小
 = 'dist\雌堕合欢宗\index.html'
$c = Get-Content $f -Raw
Write-Output ('大小: ' + [math]::Round((Get-Item ).Length/1MB, 2) + ' MB')
Write-Output ('构建错误: ' + ([regex]::Matches(, 'Module parse failed')).Count)
Write-Output ('运行时断言: ' + ([regex]::Matches(, 'throw new Error')).Count + ' (通常为 2, 正常)')

# 检查 apply_patch 残留
Get-ChildItem src -Recurse -Include '*.scss','*.ts','*.vue' | Select-String '\*\*\* End Patch'

# 检查循环依赖
pnpm lint 2>&1 | Select-String 'no-cycle'

# 测试覆盖率
npx vitest run --reporter=verbose 2>&1 | Select-String 'Tests'

# 检查 schema.json 是否最新
if ((Get-Item src\雌堕合欢宗\schema.ts).LastWriteTime -gt (Get-Item src\雌堕合欢宗\schema.json).LastWriteTime) {
    Write-Output '警告: schema.json 需要重新生成, 执行 pnpm dump'
}
```


## 十、收尾阶段经验总结（2026-05-21）

### 10.1 测试文件 mock 模式规范

所有测试文件中 mock store 时，**禁止使用 `import { __mockData } from '../store'`**。正确模式：

```typescript
// ✅ 正确：mockData 定义在模块顶层，vi.mock 通过闭包引用
const mockData = reactive({
  系统: { ... },
  牝奴: { ... },
  NPC: { ... },
  道具: { 拥有: {}, 装备: { 玩家: [] as string[], ... } },
});

vi.mock('../store', () => ({
  useDataStore: () => ({ data: mockData }),
}));
```

```typescript
// ❌ 错误：TS2305 - __mockData 未导出
vi.mock('../store', () => {
  const mockData = reactive({ ... });
  return { useDataStore: ..., __mockData: mockData };
});
import { __mockData as mockData } from '../store';
```

### 10.2 数组类型推断陷阱

空数组 `[]` 会被 TypeScript 推断为 `never[]`。mock 数据中的数组必须显式标注：

```typescript
道具: { 装备: { 玩家: [] as string[], 白芷: [] as string[], ... } }
```

遍历固定 NPC 列表时使用 `as const`：

```typescript
for (const npc of ['白芷', '苏芸', '纪兰', '沈月秋', '柳素衣'] as const) {
  mockData.NPC[npc].状态 = '已完成'; // ✅ 类型安全
}
```

### 10.3 DOM 类型断言

`document.querySelector()` 返回 `Element | null`，不支持 `.style`。需要断言为 `HTMLElement`：

```typescript
const el = document.querySelector(selector) as HTMLElement | null;
if (!el) return;
el.style.setProperty('--gradient-center-x', centerX + '%');
```

### 10.4 子 Agent 编码陷阱

- 子 agent 使用 Python Unicode 转义时，`牝`(U+7260) 和 `牡`(U+7261) 容易混淆
- PowerShell heredoc 中的正则表达式容易被转义破坏
- 推荐方案：用 `[System.IO.File]::WriteAllText()` 写 Python 脚本文件，再 `python fix.py` 执行


### 11.1 esbuild 独立脚本构建

本项目有 2 个独立 JS 文件需要用 esbuild 单独构建（webpack 不处理它们）：

- `dist/var_structure.js` — 注册 Zod Schema 到 MVU 框架
- `dist/backend_validate.js` — 后端变量校验

**构建步骤：**
1. 运行 esbuild 命令（见 `前端构建修复指南.md`）
2. `backend_validate.js` 需要 post-build 修补：替换 lodash import + 移除 export
3. 验证：控制台出现 `变量结构注册成功` 且无 404

**未构建的症状：** Zod 验证错误（支配次数 NaN、NPC undefined）
---

> 最后更新：2026-05-22
## 十二、v4 实机闭环协作规范（2026-05-22）

### 12.1 UI 交互与 AI 回复边界

- 前端 UI 是变量显示端和交互端，只能写入 `系统.待处理交互` 或执行已确认的即时本地表现，不允许直接调用 `generate()`。
- 用户发送下一条消息后，AI 才读取 `系统.待处理交互`，在新楼层自然叙事承接，并通过 JSON Patch 更新 MVU 变量。
- AI 不得重复执行前端已经完成的数据变化，例如重复扣灵石、重复增加背包库存、重复插入装备。
- 前端的“心防反震”“按钮抖动”“归零湮灭”等只属于视觉反馈；实际好感度扣减、心音揭示、剧情解锁仍由下一楼层 AI Patch 决定。

### 12.2 变量输出格式硬约束

AI 变量更新必须使用完整标签块，裸 JSON Patch 无法保证被 MVU 解析器正式应用：

```xml
<UpdateVariable>
<Analysis>Handled pending UI interactions and updated MVU state.</Analysis>
<JSONPatch>
[
  { "op": "replace", "path": "/NPC/白芷/soul_whisper/text", "value": "……" },
  { "op": "replace", "path": "/NPC/白芷/soul_whisper/is_revealed", "value": true },
  { "op": "replace", "path": "/系统/待处理交互", "value": [] }
]
</JSONPatch>
</UpdateVariable>
```

禁止只输出 `变量更新情况`、YAML 摘要或裸数组。实机已验证：裸数组会留在正文里，但 `soul_whisper` 不会回流到最新 MVU 状态。

### 12.3 场景枚举协作约束

- 当前前端场景枚举只接受 `莲灯前苑`、`醉玉小筑`、`绮梦幽阁`。
- AI 可以在正文里描写“外门广场”等叙事地点，但写入 MVU 的 `系统.当前场景` 与 `NPC.*.当前场景` 必须映射回三大 UI 场景之一。
- 新增场景前必须同步修改 `schema.ts`、`schema.json`、场景守卫、页面测试、变量列表与世界书规则，不允许只改正文。

### 12.4 文档留痕要求

任何实机验收失败都必须记录三件事：
1. CDP 读取到的最新楼层号与 iframe id。
2. 正文输出格式是否包含 `<UpdateVariable>` / `<JSONPatch>`。
3. `Mvu.getMvuData({ type: 'message', message_id: 'latest' })` 中对应变量是否实际回流。
### 12.5 世界书/角色卡同步断点（当前必须显式确认）

当前仓库中存在同步链路风险：`package.json` 仍声明 `"sync": "node tavern_sync.mjs"`，但工作区缺少 `tavern_sync.mjs`。因此：

- 不得把 `pnpm sync` 写入交付步骤并默认认为可用。
- 不得把 `pnpm build` 误认为世界书/角色卡已进入 SillyTavern。
- 本地 `src/雌堕合欢宗/世界书/变量/变量输出格式.yaml` 的修改，只有在重新 bundle/import 到酒馆实际启用角色卡或世界书后，才算进入实机环境。
- 恢复同步工具前，验收记录必须写清“同步方式”：手工导入、替代脚本、还是恢复后的 `tavern_sync.mjs`。

### 12.6 v4 实机验收最小三件套

每次验证 UI → AI → MVU 闭环，至少记录：

1. 最新楼层号与状态栏 iframe id，例如 `#6` / `TH-message--6--0`。
2. 最新正文是否包含完整 `<UpdateVariable>` 与 `<JSONPatch>`，而不是 `变量更新情况`、YAML 摘要或裸 JSON Patch。
3. `Mvu.getMvuData({ type: 'message', message_id: 'latest' })` 中目标变量是否真实变化，并确认 `系统.待处理交互` 已被清空。

只有三件套全部通过，才能判定前端交互真正影响了下一楼层游戏进程。
